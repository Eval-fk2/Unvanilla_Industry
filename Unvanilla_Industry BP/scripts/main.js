import { World, System } from '@minecraft/server';
import { ActionFormData, ModalFormData } from '@minecraft/server-ui';

export const worldUnits = [];
export const powerNetworks = [];
export const unlockedRecipes = {};
